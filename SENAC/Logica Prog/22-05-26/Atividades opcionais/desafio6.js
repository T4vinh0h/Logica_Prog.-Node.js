// 6. Cálculo de IMC com classificação
// Peça ao usuário:
//  peso em quilogramas;
//  altura em metros.
// Calcule o IMC:
// imc = peso / (altura * altura)
//
// Depois, classifique:
// imc < 18.5
// imc >= 18.5 && imc < 25
// imc >= 25 && imc < 30
// imc >= 30
//
// O programa deve mostrar:
//  o valor do IMC;
//  a classificação;
//  se a pessoa está dentro da faixa considerada normal.
//
// Desafio extra:
// Em todos os exercícios, use conversão de tipo com:
// Number(prompt("Digite um valor:"))

var prompt = require('prompt-sync')();

// Escreva seu código abaixo:

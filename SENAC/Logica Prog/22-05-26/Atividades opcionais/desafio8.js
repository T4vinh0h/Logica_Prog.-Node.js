// 8. Sistema de acesso com idade, senha e autorização
// Peça ao usuário:
//  idade;
//  senha;
//  se possui autorização especial: "sim" ou "nao".
// O usuário poderá acessar se:
// idade >= 18 && senha === "1234"
//
// Ou se possuir autorização especial:
// autorizacao === "sim"
//
// A condição final deve considerar:
// (idade >= 18 && senha === "1234") || autorizacao === "sim"
//
// O programa deve mostrar se o acesso foi liberado ou negado.
//
// Desafio extra:
// Em todos os exercícios, use conversão de tipo com:
// Number(prompt("Digite um valor:"))

var prompt = require('prompt-sync')();

// Escreva seu código abaixo:

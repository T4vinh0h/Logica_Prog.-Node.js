// 15. Criando um vetor com o triplo
// Crie o vetor:
// let numeros = [1, 3, 5, 7];
// Use map() para criar um novo vetor com o triplo de cada número.
// Resultado esperado:
// [3, 9, 15, 21]

let numeros = [1, 3, 5, 7];
let triplo = numeros.map(numero => numero * 3);

console.log(triplo);

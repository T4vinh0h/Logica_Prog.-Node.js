// 29. Copiando o vetor inteiro
// Crie o vetor:
// let cores = ["azul", "verde", "amarelo"];
// Use slice() para criar uma cópia do vetor inteiro.
// Depois, mostre:
// O vetor original;
// O vetor copiado.

let cores = ["azul", "verde", "amarelo"];
let Copia = cores.slice();

console.log(cores);
console.log(coresCopia);

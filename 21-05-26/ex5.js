// 5. Verifique se duas variáveis são diferentes usando `!=`.

let senha = 1234;
let tentativa = +(prompt(`Digite a Senha:`));

if (tentantiva != senha) {
    console.log(`Credencial Incorreta!`);
} else {
    console.log(`Acesso Permitido`);
}

// Criar um array com 5 numeros e imprimir (console.log) a soma de todos os elementos
let numeros = [10, 20, 30, 40, 50];

let soma = 0;
for (let i = 0; i < numeros.length; i++) {
    soma =soma + numeros ;
}
console.log(`Soma dos elementos: ${soma}`);

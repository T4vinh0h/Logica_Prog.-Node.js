// 5. Conversor de moedas com comparação de valores
// Peça ao usuário:
//  valor em reais;
//  cotação do dólar.
// Converta reais para dólares:
// dolares = reais / cotacao
//
// Depois, verifique se o valor convertido é:
//  menor que 100 dólares;
//  igual a 100 dólares;
//  maior que 100 dólares.
// Use: <, >, ==, >=, <=
//
// Desafio extra:
// Em todos os exercícios, use conversão de tipo com:
// Number(prompt("Digite um valor:"))

var prompt = require('prompt-sync')();

// Escreva seu código abaixo:
